<% if (currPost.tags) { %>
<% if (currPost.tags.length) { %>
<span class="post-tags">
    <% currPost.tags.forEach(function (tag) { %>
        <span class="post-tag"><a href="<%- url_for(tag.path) %>">#<%- tag.slug %></a></span>
        <% }) %>
</span>
<% } %>
<% } %>