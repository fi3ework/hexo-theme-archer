<main class="main archive-page">
  <section class="archive">
    <div class="post-content-box">
      <% if (is_archive()) { %>
      <% var resortAllPosts = site.posts.map(function (item) { %>
      <% let title = item.title; %>
      <% let link = url_for(item.path) ; %>
      <% let _date = date(item.date, "YYYY-MM-DD HH:MM:SS"); %>
      <% let returnDate = {
          title : title, 
          link : link,
          date : _date }; %>
      <% return returnDate; %>
      <% }) %>
      <% resortAllPosts.sort(function compare(v1, v2){ %>
      <% let result = (new Date(v1.date)) > (new Date(v2.date)); %>
      <%  return result == true ? -1 : 1; %>
      <% }) %>
      <% } %>
      <% let showYear = NaN %>
      <% let currPagePosts %>
      <% if (is_archive()) { %>
        <% currPagePosts = resortAllPosts; %>
      <% } else {%>
        <% currPagePosts = page.posts; %>
      <% } %>
      <div class="total-archive"> Total : <%= currPagePosts.length %> </div>
      <% let toCloseUl = 0 %>
      <div class="post-archive">
        <% currPagePosts.forEach(function (post) { %>
        <% if (showYear != Number(date(post.date, "YYYY"))) { %>
        <% showYear = date(post.date, "YYYY"); %>
        <% if (toCloseUl === 1) { %>
        <% toCloseUl = 0; %>
          </ul>
        <% } %>
        <div class="archive-year"> <%= showYear %> </div>
        <ul class="year-list">
        <% toCloseUl = 1; %>
        <% } %>
            <li class="post-item">
              <span class="post-date"><%-date(post.date, "YYYY-MM-DD") %></span>
              <a class="post-title" href= "<%- post.link %>" %>
                <%= post.title %>
              </a>
            </li>
        <% }) %>
      </div>
    </div>
  </section>
</main>